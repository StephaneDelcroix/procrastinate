<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:strings="clr-namespace:procrastinate.Resources.Strings"
             x:Class="procrastinate.Pages.GamesPage"
             Title="{Binding MiniGames, Source={x:Static strings:AppStrings.Instance}}"
             BackgroundColor="{DynamicResource SurfaceBackground}">

    <Shell.TitleView>
        <Grid ColumnDefinitions="*,Auto">
            <Label Text="{Binding MiniGames, Source={x:Static strings:AppStrings.Instance}}" 
                   FontSize="18" FontAttributes="Bold" 
                   TextColor="{DynamicResource Gray100}"
                   VerticalOptions="Center"/>
            <Button Grid.Column="1" Text="&#xf013;" 
                    FontFamily="FontAwesomeSolid"
                    BackgroundColor="Transparent"
                    TextColor="{DynamicResource Gray300}"
                    FontSize="20"
                    Clicked="OnSettingsClicked"
                    VerticalOptions="Center"/>
        </Grid>
    </Shell.TitleView>

    <ScrollView>
        <VerticalStackLayout Padding="24" Spacing="20">
            <Label Text="&#xf11b;" 
                   FontFamily="FontAwesomeSolid"
                   FontSize="48" 
                   TextColor="{DynamicResource Secondary}"
                   HorizontalOptions="Center"/>
            <Label Text="{Binding MiniGames, Source={x:Static strings:AppStrings.Instance}}" 
                   FontSize="28" 
                   FontAttributes="Bold"
                   TextColor="{DynamicResource Secondary}"
                   HorizontalOptions="Center"/>
            
            <Label Text="{Binding ProductivityOverrated, Source={x:Static strings:AppStrings.Instance}}"
                   FontSize="14"
                   TextColor="{DynamicResource Gray400}"
                   HorizontalOptions="Center"/>

            <Button x:Name="ShuffleBtn" 
                    FontSize="16"
                    BackgroundColor="{DynamicResource CardBackgroundAlt}"
                    TextColor="{DynamicResource Gray200}"
                    CornerRadius="12"
                    HeightRequest="44"
                    Clicked="OnShuffleClicked"/>

            <VerticalStackLayout x:Name="GamesContainer" Spacing="20"/>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
