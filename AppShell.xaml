<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="procrastinate.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:procrastinate"
    xmlns:pages="clr-namespace:procrastinate.Pages"
    xmlns:strings="clr-namespace:procrastinate.Resources.Strings"
    Title="Procrastinator's Helper"
    FlyoutBehavior="Flyout">

    <Shell.Resources>
        <ResourceDictionary>
            <Style TargetType="Shell" ApplyToDerivedTypes="True">
                <Setter Property="Shell.BackgroundColor" Value="{StaticResource SurfaceBackground}"/>
                <Setter Property="Shell.ForegroundColor" Value="{StaticResource Gray100}"/>
                <Setter Property="Shell.TitleColor" Value="{StaticResource Gray100}"/>
                <Setter Property="Shell.TabBarBackgroundColor" Value="{StaticResource CardBackground}"/>
                <Setter Property="Shell.TabBarForegroundColor" Value="{StaticResource Gray400}"/>
                <Setter Property="Shell.TabBarTitleColor" Value="{StaticResource Gray400}"/>
                <Setter Property="Shell.TabBarUnselectedColor" Value="{StaticResource Gray500}"/>
                <Setter Property="Shell.FlyoutBackgroundColor" Value="{StaticResource CardBackground}"/>
            </Style>
        </ResourceDictionary>
    </Shell.Resources>

    <FlyoutItem FlyoutDisplayOptions="AsMultipleItems">
        <ShellContent Title="{Binding TabTasks, Source={x:Static strings:AppStrings.Instance}}" ContentTemplate="{DataTemplate pages:TasksPage}" Route="TasksPage">
            <ShellContent.Icon>
                <FontImageSource Glyph="&#xf46d;" FontFamily="FontAwesomeSolid" Color="{StaticResource Gray400}"/>
            </ShellContent.Icon>
        </ShellContent>
        <ShellContent Title="{Binding TabGames, Source={x:Static strings:AppStrings.Instance}}" ContentTemplate="{DataTemplate pages:GamesPage}" Route="GamesPage">
            <ShellContent.Icon>
                <FontImageSource Glyph="&#xf11b;" FontFamily="FontAwesomeSolid" Color="{StaticResource Gray400}"/>
            </ShellContent.Icon>
        </ShellContent>
        <ShellContent Title="{Binding TabExcuses, Source={x:Static strings:AppStrings.Instance}}" ContentTemplate="{DataTemplate pages:ExcusePage}" Route="ExcusePage">
            <ShellContent.Icon>
                <FontImageSource Glyph="&#xf630;" FontFamily="FontAwesomeSolid" Color="{StaticResource Gray400}"/>
            </ShellContent.Icon>
        </ShellContent>
        <ShellContent Title="{Binding TabStats, Source={x:Static strings:AppStrings.Instance}}" ContentTemplate="{DataTemplate pages:StatsPage}" Route="StatsPage">
            <ShellContent.Icon>
                <FontImageSource Glyph="&#xf201;" FontFamily="FontAwesomeSolid" Color="{StaticResource Gray400}"/>
            </ShellContent.Icon>
        </ShellContent>
    </FlyoutItem>

</Shell>
